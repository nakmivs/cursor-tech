# Git Quick Commit

快速将所有更改暂存并提交。

---

请执行以下命令查看当前更改：

```bash
git status -s && git diff --stat
```

然后根据更改内容，帮我生成一个简洁的 commit message 并执行提交。提交信息格式要求：
- 使用英文
- 以动词开头（如 Add, Fix, Update, Refactor）
- 不超过 50 个字符
